version: "3.3"
services:
  niginx:
    build:
      dockerfile: Dockerfile
      context: ./nginx
    ports:
      - "80:80"
  client:
    build:
      dockerfile: Dockerfile
      context: ./client
    container_name: property-client
  server:
    build:
      dockerfile: Dockerfile
      context: ./server
    container_name: property-server
    env_file:
      - ./server/.env
